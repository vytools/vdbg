x-vy:
  anchors:
    PATH: repo:git|github.com|NateBu|vdbg
services:
  server:
    command: python3 /vdbg/test/testserver.py --directory /vdbg
    image: python:3.10-slim
    ports:
      - 80:80
    volumes:
      - $PATH:/vdbg:ro
  
version: '3.6'
  
